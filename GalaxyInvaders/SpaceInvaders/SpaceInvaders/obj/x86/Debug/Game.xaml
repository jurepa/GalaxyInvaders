<Page
    x:Class="SpaceInvaders.Game"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpaceInvaders"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SpaceInvaders.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:VMGame></vm:VMGame>
    </Page.DataContext>

    <Grid x:ConnectionId='1' x:Name="grid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"                             >
        <Border BorderBrush="Blue" BorderThickness="3" MaxHeight="600" MaxWidth="1000" Margin="10">
            <Canvas MaxHeight="600" MaxWidth="1000">
                <Image x:ConnectionId='2' x:Name="player" Source="ms-appx:///Assets/Images/AlienNavePro.png" Height="58" Width="72"
                       Canvas.Left="{Binding posX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Canvas.Top="534"  />
            </Canvas>
        </Border>
        <!--<TextBox x:Name="text" KeyDown="text_KeyDown" />-->
    </Grid>
</Page>

